{% extends 'base.html.twig' %}

{% block body %}

    <aside class="sidebar">
        <h3 class="sidebar-title">Archibald</h3>

        <nav class="sidebar-menu">
            <div class="menu-item">
                <a href="{{ path('home') }}">
                    Tendances
                </a>
            </div>
            <div class="menu-item">
                <a href="{{ path('app_login') }}">
                    Se connecter
                </a>
            </div>
            <div class="menu-item">
                <a href="{{ path('app_register') }}">
                    S'inscrire
                </a>
            </div>
        </nav>
    </aside>

    <div class="main-content">

	    {% block content %}
        {% endblock %}

    </div>

{% endblock %}
