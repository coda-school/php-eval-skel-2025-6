{% extends path %}

{% block title %}Accueil - Archibald{% endblock %}

{% block content %}


<hgroup class="main-title">
    <h1>Bienvenue sur Archibald.fr !</h1>
    {% if isUserLogged %}
        <h4>Tendances</h4>
    {% else %}
        <h4>Consultez les tendances ou connectez vous à votre compte</h4>
    {% endif %}
</hgroup>

<div class="unlogged-feed">
    {% for ver in trendingVers %}
        <div class="trending ver">
            <div>
            <strong>{{ ver.username }}</strong></div>
            <div class="link-to-ver" data-id="{{ ver.id }}">
            {{ ver.content }}</div>
            <div class="ver-stats">
                Likes : {{ ver.likes }}
                Commentaires : {{ ver.comments }}
                Partages : {{ ver.shares }}
                - {{ ver.date|date('d/m/Y H:i') }}
            </div>

        </div>
    {% endfor %}
</div>
{% endblock %}
